const http = require("http"); http.get("http://localhost:8080/api/employees", (res) => { let data = ""; res.on("data", (chunk) => data += chunk); res.on("end", () => console.log(res.statusCode, data.substring(0, 500))); }).on("error", (err) => console.log(err.message));
